import PropertyText from "../../../Components/PropertyText/PropertyText";

const TextContent = () => {
  const heading = "Popular properties";

  return (
    <div>
      <PropertyText text={heading} />
    </div>
  );
};

export default TextContent;
